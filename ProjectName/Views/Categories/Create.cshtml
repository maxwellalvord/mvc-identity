@model ProjectName.Models.Category

<div class="container">
<h4>Add a new Category</h4>
    <div id="table">
        @using (Html.BeginForm())
        {
            @Html.LabelFor(model => model.Name)
            @Html.TextBoxFor(model => model.Name, new { required = "required" })<br>

            <br><input type="submit" value="Add new Category" class="myCssClass" />   
        }
    </div>
<br>
<p>@Html.ActionLink("Show all Categories", "Index")</p>
</div>